# decks/

Slide decks and other presentation assets. Everything in `static/` is
copied verbatim into the built site, so a file here is served at
`/decks/<name>`. Link it from a talk entry in `content/talks/` — see
`../../../.agents/skills/portfolio-presenting/SKILL.md`.
