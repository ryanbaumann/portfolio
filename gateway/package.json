{
  "name": "trails-ninja-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency Node gateway: static app hosting + secret-bearing API proxy for trails.ninja.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
